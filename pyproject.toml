[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "code-server-mcp"
version = "0.1.0"
description = "MCP server for code-server (VS Code in browser)"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.0",
    "httpx>=0.27",
]
